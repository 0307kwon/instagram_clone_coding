extends layout

block content
    h1=title
    h3(style="display:inline; margin-right:5px") 
        a(href=`/`) back
    p post를 생성합니다.
    form(action="/posts/create/process" method="POST" enctype="multipart/form-data")
        p
            input(type="text" name="title" placeholder="제목을 입력해주세요.")
        p
            textarea(name="contents" cols="30" rows="10")
        p
            input(type="file" name="uploaded_file") 
        button(type="submit") 작성